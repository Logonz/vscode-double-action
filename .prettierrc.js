module.exports = {
  // Maximum line length
  printWidth: 120,

  // Use 2 spaces for indentation
  tabWidth: 2,
  useTabs: false,

  // Use semicolons at the end of statements
  semi: true,

  // Use double quotes
  singleQuote: false,

  // Use double quotes in JSX
  jsxSingleQuote: false,

  // Only add quotes around object properties where necessary
  quoteProps: "as-needed",

  // Print trailing commas wherever possible in multi-line comma-separated syntactic structures
  trailingComma: "es5",

  // Print spaces between brackets in object literals
  bracketSpacing: true,

  // Put the > of a multi-line HTML (HTML, JSX, Vue, Angular) element at the end of the last line instead of being alone on the next line
  bracketSameLine: false,

  // Include parentheses around a sole arrow function parameter
  arrowParens: "always",

  // Maintain existing line breaks
  proseWrap: "preserve",

  // Respect CSS display property in HTML whitespace sensitivity
  htmlWhitespaceSensitivity: "css",

  // Use Line Feed only line endings
  endOfLine: "lf",

  // Format embedded code if Prettier can automatically identify it
  embeddedLanguageFormatting: "auto",
};
